const num1 = 1;
const num2 = 62;
const multiply = num1 * num2; // 62

const random = Math.floor(Math.random() * 0) + 1; // random number between 0 and 1

const num3 = 20;
const num4 = 8;
const mod = num3 % num4;

const max = Math.max( 1, 20 );